name : collabcanvas 

on :
 push : 
  branches : [main] 
 
jobs : 
   build : 
     runs-on : 
     steps : 
          - name : checkout code 
            uses : actions/checkout@v4

          - name: Say hello
            run: echo "Hello from GitHub Actions! CollabCanvas build starting..."

          - name: Check Node version
            run: node --version